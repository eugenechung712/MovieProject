import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | list movies', function(hooks) {
  setupApplicationTest(hooks);
  test('should show search bar where user can search for movies', async function (assert) {
  });
  test('should show preselected movies', async function (assert) {
  });
  test('should link to movie details', async function (assert) {
  });
});
